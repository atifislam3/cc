import 'package:flutter/material.dart';

class AddMoodScreen extends StatelessWidget {
  const AddMoodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('add mood screen'),
      ),
      body: const Center(
        child: Text('add mood screen'),
      ),
    );
  }
}
