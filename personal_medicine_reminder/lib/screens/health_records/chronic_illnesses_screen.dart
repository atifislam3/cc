import 'package:flutter/material.dart';

class ChronicIllnessesScreen extends StatelessWidget {
  const ChronicIllnessesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('chronic illnesses screen'),
      ),
      body: const Center(
        child: Text('chronic illnesses screen'),
      ),
    );
  }
}
